# Increase worker timeout so flashcard generation can finish
timeout = 300       # 5 minutes
graceful_timeout = 300
keepalive = 5
workers = 1         # Keep it simple for Render free tier
threads = 1
